export enum AppRoute {
  Main = '/',
  Catalog = '/catalog',
  // Contacts = '/contacts',
  // Quest = '/quest/:id',
  // Booking = '/quest/:id/booking',
  // MyQuests = '/my-quests',
  NotFound = '/not-found',
}
