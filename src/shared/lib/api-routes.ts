export enum APIRoute {
  Catalog = '/cameras',
  // Canceling = '/reservation/:reservationId',
  // Quests = '/quest',
  // Quest = '/quest/:questId',
  // Booking = '/quest/:id/booking',
  // Login = '/login',
  // Logout = '/logout',
}
