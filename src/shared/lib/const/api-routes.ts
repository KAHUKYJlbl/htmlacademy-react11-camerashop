export enum APIRoute {
  Catalog = '/cameras',
  Banner = '/promo',
  Camera = '/cameras/:cameraId',
  // Quest = '/quest/:questId',
  // Booking = '/quest/:id/booking',
  // Login = '/login',
  // Logout = '/logout',
}
